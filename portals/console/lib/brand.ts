/**
 * Console brand metadata. The header/footer chrome renders identically to the
 * website portal because both draw from the shared `ruyinBrandCore`.
 */
export { markSrc } from "@umbra/shared/brand";
import { ruyinBrandCore } from "@umbra/shared/brand";

export const ruyinBrand = ruyinBrandCore;
