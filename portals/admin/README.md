# Ruyin Admin Portal

Temporary management surface for Ruyin VPN administration.

Current production routing keeps Marzban at `console.ruyin.ai/dashboard/` and
serves invite management through the Next app in `portals/console`. The invite
UI should move here after the user console and admin routes are separated.
